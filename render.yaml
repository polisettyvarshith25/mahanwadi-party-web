services:
  - type: web
    name: mahanwadi-web-portal
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
